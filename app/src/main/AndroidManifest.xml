<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Examify"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".SignInActivity" />
        <activity android:name=".SignUpActivity" />
        <activity
            android:name=".TeacherActivity"
            android:exported="true">
        </activity>
        <activity android:name=".InstitutionActivity" />
        <activity android:name=".StudentActivity" />
        <activity android:name=".StudentEnrollmentActivity" />
        <activity android:name=".TeacherEnrollmentActivity" />
        <activity android:name=".ModuleActivity" />
        <activity android:name=".QuizInterface" />
        <activity android:name=".MCQEditorActivity" />
        <activity android:name=".QuizFeedbackActivity" />
        <activity android:name=".AssignmentLauncherActivity" />
    </application>

</manifest>